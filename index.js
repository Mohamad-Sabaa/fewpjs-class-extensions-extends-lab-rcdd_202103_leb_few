// Your code here

class Polygon {
  constructor (side) {
    this.side = side;
    this.count = this.side.length;
  }
  get countSides() {
    return this.side.length;
  }
  get perimeter(side) {
    let count = 0;
    for (i=0 ; i < side.length ; i++)
    return this.count;
  }
}
